在ygopro根目录创建一个名为chronicle的文件夹
创建chronicle/decks，把卡组文件复制到里面
创建chronicle/index.txt，在其中写入卡组文件名称，每行一个，完整文件名（如aaa.ydk），不要空行，注意使用当前操作系统的 默 认 字 符 集 保存！（Windows用GBK，Linux用UTF-8）
把维护工具放进卡组开一局，发动其效果即可在chronicle目录生成c13959972.lua

注：严格模式，开启时，会抛弃存在问题的卡组，如卡片不存在的，数量不符合规定的。关闭时，只会忽略不存在的卡，卡组照常保存。